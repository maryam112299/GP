# Copy to .env (this dir loads it automatically if you `dotenv run`).
# Or just set them in the shell where you launch the agents.

GROQ_API_KEY=gsk_replace_me_with_your_key
GROQ_MODEL=llama-3.1-8b-instant

# Ports
BANK_AGENT_PORT=9100
HR_AGENT_PORT=9200
